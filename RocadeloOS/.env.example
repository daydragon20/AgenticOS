# RocadeloOS secrets — kopieer naar .env (staat in .gitignore, gaat NOOIT mee de repo in)

# AI / pipeline
ANTHROPIC_API_KEY=
UNIPILE_DSN=
UNIPILE_TOKEN=
NODEMAVEN_USER=
NODEMAVEN_PASS=

# GitHub read-only toegang voor AI's/automations (privé-repo lezen zonder je interactieve login)
# Fine-grained PAT, alleen deze repo, Contents=Read-only, met vervaldatum. Zie ACCESS-VOOR-AI.md.
GITHUB_READ_TOKEN=
GITHUB_REPO=daydragon20/agenticOS
