Fase -1 Setup & Billing: Anthropic Console + $5–10 credit; API-key in .env; `pip install anthropic python-dotenv requests schedule` (+ supabase of stdlib sqlite3); 10-regel API-test; kosten ~$3–5/mnd/klant. Bouwmethode (micro-stappen per Claude Code-stap): voorbereiding → verificatie (testcommando + verwachte output + veelvoorkomende errors) → 3–5 micro-commands (lege klasse+imports → DB-connectie → kernfunctie bv score_lead() → logging → test) → integratietest. Werkverdeling: webapp Claude = ontwerpen (teken eerst 7-agent-graaf "Brok 0"); Claude Code = bouwen (n8n-mcp → lokale n8n). Datalaag SQLite (start) → Supabase (schaal). Robuust: webhooks 30s/200 async, rate limits, retries/backoff, idempotent, logging per agent/lead.
