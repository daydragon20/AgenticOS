Start CX23 (2/4) of CAX11 (ARM); EU/GDPR. Opzet: Ubuntu LTS via console/hcloud; stateful firewall (gratis, alleen nodige poorten); Docker → n8n (persistent volume, basic auth, HTTPS via Caddy); Python venv/containers voor agents; .env op server; backups/snapshots. Automatiseren via API/CLI/Terraform/Ansible. Proxies (NodeMaven) op outbound LinkedIn-calls, niet de server; monitoring op agent-fouten + account-status (ban-detectie).
